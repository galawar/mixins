include _vars
include _mixins

- array = '';
block vars

doctype
html(lang="en")
	head
		+head(array)

	body(class= array.body_class)
		.wrapper(class= array.layout_mod)
			.base(class= array.layout_mod)
				block content_block
			//-+footer
		//-+scripts
		block local_popup
